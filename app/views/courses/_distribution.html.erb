<i><%=link_to distribution.name, distribution, class: 'btn btn-link' %></i>
<%= button_to 'Not this distribution!', {controller: "courses", action: "delete_distribution", distribution: distribution, method: 'get'},
    {:confirm => 'Are you sure?',
    :method => :delete, class: 'btn'} if current_user.present? && (current_user.has_role? :admin)%>
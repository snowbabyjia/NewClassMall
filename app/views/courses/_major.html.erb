<i><%=link_to major.name, major %></i>
<%= button_to 'Not this major!', {controller: "courses", action: "delete_major", :major => major, method: 'get'},
    :confirm => 'Are you sure?',
    :method => :delete if current_user.present? && (current_user.has_role? :admin)%>
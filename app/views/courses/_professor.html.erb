<div class="comment">	
    <%= link_to professor.name, professor, class: 'btn btn-link'%>
    <%= button_to 'Not this professor', {:action => "delete_course", :course_id => @course.id, :professor_id => professor, :method => 'get'},
      {:confirm => "Are you sure?", 
      :method => :delete, class:'btn'} if current_user.present? && (current_user.has_role? :admin)
      %>
</div>
